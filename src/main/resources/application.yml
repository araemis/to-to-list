spring:
      application:
            name: to-do-list
      datasource:
            url: jdbc:mysql://localhost:3306/TODO
            username: root
            password: root
            driver-class-name: com.mysql.cj.jdbc.Driver
      jpa:
            hibernate:
                  ddl-auto: update
            show-sql: true

